name: Generate Snake

on:
  schedule:
    # Run once every 24 hours
    - cron: "0 0 * * *"
  
  # Allow manual execution in the Actions tab
  workflow_dispatch:
  
  # Run on every push to the main branch
  push:
    branches:
    - main

jobs:
  generate:
    permissions: 
      contents: write
    runs-on: ubuntu-latest
    timeout-minutes: 5
    
    steps:
      # Generates a snake game from a github user's contributions graph, outputting a SVG animation
      - name: Generate github-contribution-grid-snake.svg
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: ayushX15
          outputs: |
            dist/github-contribution-grid-snake.svg?color_snake=#00f2fe&color_dots=#161b22,#0d2d3a,#0e536b,#0089a8,#00f2fe
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark&color_snake=#ff007f&color_dots=#161b22,#3c0022,#6d003d,#bd006a,#ff007f
          
      # Push the generated SVGs to the 'output' branch of the repository
      - name: Push github-contribution-grid-snake.svg to the output branch
        uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
